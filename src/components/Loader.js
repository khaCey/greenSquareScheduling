// Loader.js
import React from 'react';
import { LoaderWrapper, Spinner } from './LoaderStyledComponents';

const Loader = () => {
  return (
    <LoaderWrapper>
      <Spinner />
    </LoaderWrapper>
  );
};

export default Loader;
